<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    >

    <item
        android:id="@+id/setting"
        android:iconTint="@android:drawable/ic_menu_preferences"
        android:title="Setting"
        />
    <item
        android:id="@+id/status"
        android:iconTint="@android:drawable/status_bar_item_app_background"
        android:title="Status" />
    <item
        android:id="@+id/exit"
        android:iconTint="@android:drawable/ic_menu_close_clear_cancel"
        android:title="Exit" />
</menu>